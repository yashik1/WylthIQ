/**
 * The brand's own words, in one place.
 *
 * Three things say the slogan — the hero, the share card, and the footer —
 * and they are three different rendering paths: a React component, a Satori
 * image, and the root layout. A string repeated across those is a string that
 * eventually disagrees with itself, and the disagreement shows up on a shared
 * link rather than anywhere anybody would look.
 */

export const BRAND = "WylthIQ";

/**
 * The slogan.
 *
 * Deliberately about understanding rather than about returns. This product
 * describes what companies filed and never recommends acting on it — the
 * scoring code has a test that fails the build if a health report so much as
 * says "buy" — so a slogan promising gains would be writing a cheque the
 * whole codebase is built to refuse. "Before" is doing the work: it puts this
 * upstream of a decision rather than in it.
 */
export const SLOGAN = "Understand before you invest.";
